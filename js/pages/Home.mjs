const HomePage = document.createElement('main');

HomePage.appendChild(document.createTextNode('Home page'));
HomePage.appendChild(document.createElement('br'));
HomePage.appendChild(document.createTextNode('Home page'));

export default HomePage;
